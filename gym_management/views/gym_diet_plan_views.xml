<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_gym_diet_plan_tree" model="ir.ui.view">
        <field name="name">gym.diet.plan.tree</field>
        <field name="model">gym.diet.plan</field>
        <field name="arch" type="xml">
            <list string="Diet Plans" decoration-success="state == 'active'" decoration-muted="state == 'completed'">
                <field name="name"/>
                <field name="member_id"/>
                <field name="trainer_id"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="total_calories"/>
                <field name="state" widget="badge" decoration-success="state == 'active'" decoration-warning="state == 'draft'" decoration-muted="state == 'completed'"/>
            </list>
        </field>
    </record>

    <record id="view_gym_diet_plan_form" model="ir.ui.view">
        <field name="name">gym.diet.plan.form</field>
        <field name="model">gym.diet.plan</field>
        <field name="arch" type="xml">
            <form string="Diet Plan">
                <header>
                    <button
                        name="action_activate"
                        string="Activate"
                        type="object"
                        class="btn-primary"
                        invisible="state != 'draft'"
                    />
                    <button
                        name="action_complete"
                        string="Mark as Completed"
                        type="object"
                        class="btn-success"
                        invisible="state != 'active'"
                    />
                    <button
                        name="action_reset_draft"
                        string="Reset to Draft"
                        type="object"
                        invisible="state == 'draft'"
                    />
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Diet Plan Name"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Plan Details">
                            <field name="member_id" options="{'no_create': True}"/>
                            <field name="trainer_id" options="{'no_create': True}"/>
                        </group>
                        <group string="Schedule">
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="total_calories" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Diet Items" name="diet_items">
                            <field name="diet_line_ids">
                                <list editable="bottom">
                                    <field name="diet_day"/>
                                    <field name="diet_interval_id" options="{'no_create': True}"/>
                                    <field name="food_item_id" options="{'no_create': True}"/>
                                    <field name="quantity"/>
                                    <field name="calories" readonly="1"/>
                                    <field name="notes"/>
                                </list>
                            </field>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="notes" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_gym_diet_plan_search" model="ir.ui.view">
        <field name="name">gym.diet.plan.search</field>
        <field name="model">gym.diet.plan</field>
        <field name="arch" type="xml">
            <search string="Diet Plans">
                <field name="name"/>
                <field name="member_id"/>
                <field name="trainer_id"/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Active" name="active" domain="[('state', '=', 'active')]"/>
                <filter string="Completed" name="completed" domain="[('state', '=', 'completed')]"/>
                <group>
                    <filter string="Member" name="group_member" context="{'group_by': 'member_id'}"/>
                    <filter string="Trainer" name="group_trainer" context="{'group_by': 'trainer_id'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_gym_diet_plan" model="ir.actions.act_window">
        <field name="name">Diet Plans</field>
        <field name="res_model">gym.diet.plan</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
